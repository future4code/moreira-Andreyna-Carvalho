/* const listFilter = toDoUser[0].listToDo.filter(list => {
  return list.title.toLowerCase().includes(String(req.query.title).toLowerCase())
})
const mapeamentoToDo = listFilter.map(info => {
  return {id: info.id, title: info.title}
})

type resposta = {
  quantity: Number,
  list: any[]
}
const resultado: resposta = {
  quantity: listFilter.length,
  list:mapeamentoToDo
} */